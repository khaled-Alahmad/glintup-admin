"use client";

import "leaflet/dist/leaflet.css";

// This component doesn't render anything visible
// It's just to ensure Leaflet CSS is properly loaded
export default function LeafletStyles() {
  return null;
}
